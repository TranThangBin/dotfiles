# Reference from https://github.com/nix-community/home-manager/blob/release-25.05/modules/services/wlsunset.nix
[Unit]
Description=Day/night gamma adjustments for Wayland compositors.
PartOf=graphical-session.target
Requires=graphical-session.target
After=graphical-session.target
ConditionEnvironment=WAYLAND_DISPLAY

Requires=distrobox-container@%i.service
PartOf=distrobox-container@%i.service
Conflicts=wlsunset.service

[Service]
Type=simple
ExecCondition=distrobox-enter --no-workdir %i -- sh -c 'command -v wlsunset &>/dev/null'
# Vietnam/Ho Chi Minh city
ExecStart=distrobox-enter --no-workdir %i -- wlsunset -l 10.7 -L 106.7 -t 5000 -T 6000
Slice=session.slice
Restart=on-failure
