[Unit]
Description=Timed resync
After=psd.service
Wants=psd-resync.timer
BindsTo=psd.service

[Service]
Type=oneshot
ExecStart=distrobox-enter --name trantbox-services -- /usr/bin/profile-sync-daemon resync
Environment=LAUNCHED_BY_SYSTEMD=1
